export enum ProjectType {
  pcd_only = 'pcd_only',
  pcd_image = 'pcd_image',
  pcd_image_frames = 'pcd_image_frames',
}

export enum AnnotationType {
  cuboid = 'cuboid',
}

export enum ContentResourceType {
  pcd = 'pcd',
  image_png = 'image_png',
  image_jpeg = 'image_jpeg',
}
